# Port du serveur backend
PORT=3000

# Clé secrète JWT pour l'authentification admin
# IMPORTANT: Générez une clé aléatoire sécurisée pour la production
# Exemple: utilisez "node -e "console.log(require('crypto').randomBytes(32).toString('hex'))""
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Environnement (development, production)
NODE_ENV=development

# Base de données
# Le fichier SQLite sera créé automatiquement
DB_PATH=./backend/db.sqlite

# Configuration CORS (optionnel)
# CORS_ORIGIN=https://votre-domaine.com
